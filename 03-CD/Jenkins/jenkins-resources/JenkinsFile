pipeline {
    agent any

    stages {
        stage('Audit tools') {
            steps {
                sh '''
                    git version
                    docker version
                '''
            }
        }
    }
}